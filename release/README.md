# Release control metadata

Files in this directory record explicit release approval boundaries and candidate lineage. They do not switch `update/manifest.json`, activate canonical production ownership, merge `main`, or authorize legacy deletion by themselves.
