'use strict';
module.exports={subsystem:'draft',status:'planned',migration_rule:'outputs must match v0.15.135 differential fixtures unless an approved scoring change exists',production_active:false};
