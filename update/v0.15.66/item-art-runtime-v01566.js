'use strict';
(()=>{
  const V='0.15.66';
  if(window.__ARAM_ITEM_ART_RUNTIME_V01566__)return;
  window.__ARAM_UI_REFRESH_V01560__=true;
  window.__ARAM_ITEM_ART_HOTFIX_V01563__=true;
  window.__ARAM_ITEM_ART_UNIFIED_V01564__=true;
  window.__ARAM_ITEM_ART_STABLE_V01565__=true;
  window.__ARAM_ITEM_ART_SINGLE_OWNER_V01565__=true;
  const $$=(s,r=document)=>[...(r?.querySelectorAll?.(s)||[])];
  const norm=s=>String(s??'').replace(/\s+/g,' ').trim();
  const key=s=>norm(s).toLowerCase().replace(/[\s·.'’"`():\-_/]/g,'');
  let catalog=null,pending=null,nameToId=new Map(),observer=null;
  const runtime=new Map();
  const counters={scans:0,mutationBatches:0,imagesVisited:0,itemImagesResolved:0,primaryReady:0,fallbackReady:0,missing:0,sameAssetSkips:0};
  function ensureStyle(){if(document.getElementById('aramItemArtRuntimeStyleV01566'))return;const st=document.createElement('style');st.id='aramItemArtRuntimeStyleV01566';st.textContent=`img[data-aram-item-art-switching-v01566="1"]{visibility:hidden!important;transition:none!important} img[data-aram-item-art-ready-v01566="1"]{visibility:visible!important}`;document.head.appendChild(st)}
  function setCatalog(x){catalog=x||null;nameToId=new Map();for(const [id,it] of Object.entries(catalog?.items||{})){const name=norm(it?.name);if(name)nameToId.set(key(name),String(id))}}
  async function loadCatalog(){if(catalog?.ok)return catalog;if(pending)return pending;const api=window.aramDesktop?.getItemCatalog;if(typeof api!=='function')return null;pending=Promise.resolve().then(()=>api()).then(x=>{if(x?.ok)setCatalog(x);return x||null}).catch(()=>null).finally(()=>{pending=null});return pending}
  function idFrom(img){const direct=String(img?.dataset?.itemId||'').match(/^\d+$/)?.[0];if(direct&&catalog?.items?.[direct])return direct;const holder=img?.closest?.('[data-item-id],[data-ri-icon-item-v01556],[data-aram-item-icon-v01557]');const byData=String(holder?.dataset?.itemId||holder?.dataset?.riIconItemV01556||holder?.dataset?.aramItemIconV01557||'').match(/\d+/)?.[0];if(byData&&catalog?.items?.[byData])return byData;const src=String(img?.getAttribute?.('src')||'');const bySrc=src.match(/(?:\/img\/item\/|\/item\/|\/icons2d\/)(\d+)(?:[_./?]|$)/i)?.[1]||'';if(bySrc&&catalog?.items?.[bySrc])return bySrc;for(const text of [img?.alt,img?.title]){const byName=nameToId.get(key(text));if(byName)return byName}return''}
  function candidates(id){const it=catalog?.items?.[String(id)];if(!it)return[];const xs=[it.iconPrimaryUrl,it.iconUrl,...(Array.isArray(it.iconFallbackUrls)?it.iconFallbackUrls:[])].filter(Boolean);const out=[],seen=new Set();for(const x of xs){const v=String(x);if(!seen.has(v)){seen.add(v);out.push(v)}}return out}
  function assetKey(url){try{const u=new URL(String(url||''));return `${u.hostname}${decodeURIComponent(u.pathname).toLowerCase()}`}catch{return String(url||'').split(/[?#]/)[0].toLowerCase()}}
  function sameAsset(a,b){return !!a&&!!b&&assetKey(a)===assetKey(b)}
  function versioned(url,id,index){try{const u=new URL(String(url));u.searchParams.set('aramItemArt',V);u.searchParams.set('item',String(id));u.searchParams.set('candidate',String(index));return u.toString()}catch{return String(url||'')}}
  function legacyCompat(img,id,index){img.dataset.itemId=String(id);img.dataset.aramArtV01560=String(id);img.dataset.aramCurrentArtV01563=String(id);img.dataset.aramUnifiedItemArtV01564=String(id);img.dataset.aramUnifiedCandidateV01564=String(index);img.dataset.aramItemArtCandidateV01565=String(index)}
  function record(id,state,index,url){const it=catalog?.items?.[String(id)];runtime.set(String(id),{id:String(id),name:it?.name||'',state,index,url:String(url||''),at:Date.now()})}
  function markReady(img,id,index,base){legacyCompat(img,id,index);img.dataset.aramItemArtReadyV01566='1';img.dataset.aramItemArtCandidateV01566=String(index);delete img.dataset.aramItemArtSwitchingV01566;img.style.visibility='';img.style.display='';if(index===0)counters.primaryReady++;else counters.fallbackReady++;record(id,index===0?'primary':'fallback',index,base)}
  function applyCandidate(img,id,index){const xs=candidates(id);if(!xs.length||index>=xs.length){delete img.dataset.aramItemArtSwitchingV01566;img.style.visibility='';counters.missing++;record(id,'missing',index,'');return}const base=xs[index];legacyCompat(img,id,index);img.dataset.aramItemArtSwitchingV01566='1';delete img.dataset.aramItemArtReadyV01566;const token=`${id}:${index}:${Date.now()}:${Math.random()}`;img.dataset.aramItemArtTokenV01566=token;img.onerror=()=>{if(img.dataset.aramItemArtTokenV01566!==token)return;applyCandidate(img,id,index+1)};img.onload=()=>{if(img.dataset.aramItemArtTokenV01566!==token)return;if(!sameAsset(img.currentSrc||img.src,base)){delete img.dataset.aramItemArtSwitchingV01566;refreshImg(img);return}markReady(img,id,index,base)};record(id,'pending',index,base);img.src=versioned(base,id,index)}
  function refreshImg(img){if(!img||img.tagName!=='IMG'||!catalog?.ok)return;counters.imagesVisited++;if(img.dataset.aramItemArtSwitchingV01566==='1')return;const id=idFrom(img);if(!id)return;counters.itemImagesResolved++;const xs=candidates(id);if(!xs.length)return;const current=String(img.currentSrc||img.getAttribute('src')||'');const own=Number(img.dataset.aramItemArtCandidateV01566??-1);if(img.dataset.aramItemArtReadyV01566==='1'&&own>=0&&xs[own]&&sameAsset(current,xs[own])){legacyCompat(img,id,own);counters.sameAssetSkips++;return}if(sameAsset(current,xs[0])){markReady(img,id,0,xs[0]);counters.sameAssetSkips++;return}applyCandidate(img,id,0)}
  function touch(node){if(!node||node.nodeType!==1)return;if(node.tagName==='IMG')refreshImg(node);$$('img',node).forEach(refreshImg)}
  function scan(root=document){if(!catalog?.ok){loadCatalog().then(x=>{if(x?.ok)scan(root)});return}counters.scans++;if(root?.tagName==='IMG'){refreshImg(root);return}$$('img',root).forEach(refreshImg)}
  function onMutations(records){counters.mutationBatches++;if(!catalog?.ok){loadCatalog().then(x=>{if(x?.ok)scan()});return}for(const rec of records){if(rec.type==='attributes'){refreshImg(rec.target);continue}for(const node of rec.addedNodes||[])touch(node)}}
  function getAuditReport(){const states=[...runtime.values()];return{version:V,singleOwner:true,legacyLayersPreempted:['0.15.60','0.15.63','0.15.64','0.15.65'],recurringFullDocumentScan:false,perSessionCacheBust:false,catalogVersion:catalog?.version||'',runtimeItems:states.length,pending:states.filter(x=>x.state==='pending').length,primaryReady:states.filter(x=>x.state==='primary').length,fallbackReady:states.filter(x=>x.state==='fallback').length,missing:states.filter(x=>x.state==='missing').length,counters:{...counters},score_logic_changed:false}}
  function refresh(){scan()}
  function start(){ensureStyle();observer=new MutationObserver(onMutations);observer.observe(document.documentElement,{childList:true,subtree:true,attributes:true,attributeFilter:['src','data-item-id','alt','title']});loadCatalog().then(x=>{if(x?.ok)scan()});window.__ARAM_ITEM_ART_RUNTIME_V01566__=true;window.__ARAM_ITEM_ART_SINGLE_OWNER_V01566__=true;const api={version:V,refresh,resolve:(idOrName)=>{const raw=String(idOrName??''),id=catalog?.items?.[raw]?raw:(nameToId.get(key(raw))||'');return id?{id,item:catalog.items[id],candidates:candidates(id)}:null},getAuditReport,getCatalog:()=>catalog,observer,score_logic_changed:false};window.aramItemArtResolverV01566=api;window.aramItemArtResolverV01565=api;window.aramItemArtResolverV01564=api;window.aramUiRefreshV01560={version:V,refresh,delegated_item_art_owner:'v0.15.66',visible_party_label:'팀원픽',score_logic_changed:false}}
  start();
})();
