# v0.15.109 — Screenshot UI Polish

Screenshot-confirmed cleanup after the v0.15.108 boundary hardfix.

- Patch Notes mode now relabels the outer detail header from `챔피언 상세` to `패치노트` and restores the original label when returning to the tier view.
- Removes the stale internal `0.15.99` module number from the visible Patch Companion eyebrow and shows patch `26.18` instead.
- Restores the intended DNA bars, AD/AP split bars, chips, and compact candidate typography in RANDOM Practice even when older scoped layout classes fail to apply.
- Slightly widens the RANDOM Practice intel column on wide displays while preserving the TOP5 workspace.
- No recommendation score, tier, item, champion-grade, or balance logic changes.
