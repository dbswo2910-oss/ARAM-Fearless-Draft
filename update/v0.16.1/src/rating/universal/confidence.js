'use strict';
const finiteOrNull=v=>v===null||v===undefined||v===''?null:(Number.isFinite(Number(v))?Number(v):null);
function confidence({games=0,uncertainty=null,rating=null}={}){games=Math.max(0,Number(games)||0);const u=finiteOrNull(uncertainty),r=finiteOrNull(rating);if(!games||r===null)return{level:'INSUFFICIENT',label:'분석 데이터 부족',score:0,lower:null,upper:null,uncertainty:null,method:'insufficient'};const proxy=u===null?Math.max(45,360/Math.sqrt(games+1)):Math.max(0,u);const score=Math.max(0,Math.min(100,Math.round((Math.min(1,games/30)*.65+Math.max(0,1-Math.min(1,proxy/350))*.35)*100)));const level=games>=20&&proxy<=120?'HIGH':games>=8&&proxy<=220?'MEDIUM':'LOW';const label=level==='HIGH'?'높음':level==='MEDIUM'?'보통':'낮음';return{level,label,score,lower:Math.round(r-1.96*proxy),upper:Math.round(r+1.96*proxy),uncertainty:proxy,method:u===null?'sample_proxy':'estimator_uncertainty'}}
module.exports={finiteOrNull,confidence,production_active:false};