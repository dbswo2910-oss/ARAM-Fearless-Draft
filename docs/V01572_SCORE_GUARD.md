# v0.15.72 score guard

`score_logic_changed:false`

The stability release preserves the existing `teamScore(names, randomState.ourModes)` scoring source, exhaustive candidate semantics, and score-first/tie-by-party-name ordering. Only scheduling, observation scope, and rendering cadence change.
