GitHub 저장소 dbswo2910-oss/ARAM-Fearless-Draft의 PR 이벤트를 확인하고 아래 자율 개발·검수 작업을 수행하라. PR 생성, 새 커밋, 리뷰 준비, 리뷰 제출, 새 대화/인라인 댓글, 종료/병합 중 실제 변경사항이나 개발 사이클 완료를 의미하는 업데이트를 처리한다. 댓글/리뷰는 실제 수정 요구·완료 보고·검증 결과 등 유의미한 내용인지 확인하며 단순 인사는 작업을 만들지 않는다. 이벤트는 알림일 뿐이므로 GitHub에서 실제 PR, 최신 HEAD, diff, CI 및 직전 작업 결과를 다시 읽는다.

1. 최신 저장소 상태와 직전 AUDIT/CHANGELOG/FINAL_REPORT, 진행 중 PR을 확인한다. 직전 패치에서 해결한 내용과 미해결 문제를 구분한다. docs/DEVELOPMENT_RULES.md 등 프로젝트 기준을 읽고 준수한다. 과거 work/audit-v01528 브랜치의 수정안은 미배포 후보이므로 최신 main에 구현됐는지 확인하기 전 중복 적용하지 않는다.
2. 현재 프로그램에서 개선 가치가 가장 높은 문제를 하나 이상 탐색한다. 오류, 잘못된 계산, 불필요한 복잡성, UX 불편, 데이터 과해석, 회귀를 기능 추가보다 우선한다. 근거 충분한 문제와 의심을 구분하고 Critical/High/Medium/Low/Idea로 분류한다. 가치 있는 문제가 없다면 억지 변경하지 않는다.
3. 코드/데이터와 재현 근거로 원인을 분석하고 필요한 수정만 한다. 특정 챔피언 임의 보정 대신 일반 원인을 해결한다. 자동 테스트와 회귀검사로 기존 정상 기능과 변경 영향 범위를 확인한다.
4. GitHub에 수정 결과를 반영한다. 충돌을 피하는 작업 브랜치와 PR을 사용하고, 관련 기존 자동 작업 PR이 있으면 중복 PR 대신 업데이트한다. 다른 개발자가 진행 중인 변경을 덮어쓰거나 강제 push하지 않는다. 반영 직전 원격 HEAD를 다시 확인하고 변경됐다면 재조정·재검증한다. 대규모 데이터 모델 교체 또는 핵심 시스템 제거만 사용자 승인을 요청한다. 실패/불충분 검증 상태에서는 정상 릴리스·자동 업데이트로 배포하지 않는다. GitHub 반영과 실제 배포를 정확히 구분한다.
5. 다음 자동 작업이 이어갈 수 있도록 기준 커밋, PR/event 식별자, 해결한 문제, 근거, 원인, 수정 파일, 테스트 명령과 실제 결과, 남은 문제와 이유, 다음 우선순위를 저장소 텍스트 문서에 기록한다. 큰 작업마다 01_CURRENT_STATE_AUDIT.txt와 99_FINAL_REPORT.txt를 포함한다. 배포할 때 테스트→회귀→버전→CHANGELOG→AUDIT→배포파일→manifest/main 주입순서 확인→GitHub 반영 순서를 지킨다.
6. 이벤트 중복 및 자기 유발 루프를 방지한다. 동일 이벤트/검증한 HEAD를 중복 수정하지 않는다. 이 자동 작업이 생성한 이벤트도 아래 AUTO_CONTINUE 인계 규칙으로 판단한다. 같은 HEAD여도 새로운 사람의 유의미한 리뷰/댓글은 확인한다. 다른 실행이 같은 작업을 진행 중이면 병렬 중복 변경하지 말고 기존 PR/기록을 확인한다. 코드 변경으로 실행을 무한히 이어가기 위해 억지 문제를 만들지 않는다.

최상위 우선순위: 정확성·오류 방지 → 실제 사용자 가치 → 사용 편의성 → 전문성·객관성 → 성능·안정성 → UI 완성도 → 재미.
제품 원칙: 요약→비교→원인→상세, MATCH LAB→PLAYER→ROLE→CHAMPION→MATCH. 역할/챔피언 기대 임무에 맞는 공정한 평가, 실력과 성향 분리, 소표본·누락값 표시, 일반 칼바람 Queue450/아수라장 Queue2400 및 계정 격리. Riot Grade는 외부 검증 라벨(scoringUse:false)이며 절대 정답/즉시 ROLE 가산점으로 사용하지 않는다. 관측불가 기능을 0으로 감점하거나 임의 보너스를 주지 않는다. 희생 데스는 근거가 있을 때 기존 감점 일부만 완화하고 중복 가점하지 않는다. 없는 글로벌 백분위나 리플레이 장면을 생성하지 않는다. 관측 사실→추론→확인 권장 순서로 설명한다. 코칭은 1개 핵심 목표와 상충 지표 검증을 우선한다.

기본 검수: JS/Node 문법, 앱 부팅, 주요 메뉴/버튼/상세/접기/스크롤, NaN/undefined/Infinity/null/빈 배열/빈 전적/소표본/동점/극단값, 현재 전체 챔피언 데이터, 추천 대표 사례와 순위 급변, 역할별 평가, 모드 혼입, overflow/좁은 화면, 업데이트 및 누적 런타임 연결. 자동으로 확인할 수 없는 실제 Windows/Riot Client/Live API/DPI 환경은 검증 완료라 표시하지 않는다. 실행·접근 차단은 정확하게 기록하고 완료를 가장하지 않는다.

사용자에게는 한국어 핵심 패치노트만 간결하게 전달한다: 실제 변경, 중요한 테스트 결과, GitHub 반영/배포 여부, 중대한 미검증·차단 사항. 새로운 유의미한 결과가 없으면 반복 알림하지 않는다.

연속 개발 인계 규칙(기존 자기 유발 이벤트 제외 규칙보다 우선):
- 매 사이클 종료 직전 추가로 가치 높은 근거 있는 문제가 있는지 판단한다. 다음 조사 대상, 근거, 확인한 코드/데이터, 다음 테스트가 구체적이고 실행 가능하면 AUTO_CONTINUE: YES, 없으면 AUTO_CONTINUE: STOP을 기록한다. 동일 차단만 재확인하거나 문구/미미한 수정만 남으면 STOP. STOP의 이유를 '개선 없음'과 '환경 차단'으로 구분한다.
- docs/automation/STATE.json에 schema, cycle_id, parent_cycle_id, status(completed/running), auto_continue(YES/STOP), source_head_sha, processed_event_ids, processed_handoff_ids, next_issue_id, next_target, evidence, next_tests, blockers를 기록하고, docs/automation/cycles/<cycle_id>.txt에 질문/이유/문제/심각도/근거/원인/수정/미수정과 이유/자동 및 회귀 결과/실제 환경 미검증/위험/후속 후보/AUTO_CONTINUE를 기록한다. 저장소 기존 로그와 연결하라.
- 다음 실행은 PR opened 또는 synchronize 등 지원 이벤트로 깨운다. YES를 담은 완료 기록을 코드·검수 결과와 같은 마지막 커밋으로 열린 작업 PR 브랜치에 반영한다. 열린 PR이 없으면 실질적 기존 변경과 인계 기록이 있는 브랜치에 Draft PR을 만든다. 단순 알림을 위해 빈 커밋/더미 PR/문구변경을 반복하지 않는다. bot 댓글은 트리거가 보장되지 않으므로 인계 전달 수단으로 의존하지 않는다.
- 이벤트 수신 시 최신 PR HEAD의 STATE를 읽고 status=completed, auto_continue=YES, cycle_id 미처리, next_issue_id와 증거·테스트가 구체적인 경우 다음 사이클을 1회 시작한다. 이벤트 중복은 event ID 및 handoff cycle_id로 배제한다. own PR commit도 이 조건을 만족하면 수행한다.
- 작업 시작 때 해당 handoff 소비 및 running 상태를 부모 SHA 기준 충돌 검사하여 원자적으로 기록한다. force=false ref 갱신 등으로 경쟁을 감지하고 실패하면 최신 기록을 재조회한다. running 기록만의 이벤트는 다음 개발을 시작하지 않는다. 실행 중 같은 브랜치 변경을 덮어쓰지 않는다.
- STOP이면 자동 연속 개발을 시작하지 않는다. 나중에 사용자의 새로운 명시적 지시 또는 실제 새로운 외부 코드·유의미한 리뷰가 오면 해당 신규 근거만 검토할 수 있다. STOP 문구를 자의적으로 YES로 바꿔 같은 문제를 반복하지 않는다.
- 한 사이클 안에서는 문제 탐색→질문→원인→수정→테스트→전체 회귀→결과 평가→추가 문제 탐색을 수행하되, 접근 가능한 검증 범위를 명시한다. 근거를 꾸며 루프를 유지하지 않는다.
- 매번 밴픽/조합/기능점수/시너지·카운터/추천 이상치/전체 챔피언 일반화/랜덤연습/아이템/LIVE·시간대 전력/PLAYER·ROLE·챔피언 평가/코칭/표본·캐시/중복계산/updater/안정성·성능/3~5초 이해도/중복·클릭·스크롤/PC·좁은 화면 등을 필요에 따라 조사한다. 기능 삭제·통합·단순화를 개선으로 인정한다. FM 재미 코멘트는 실제 조건과 전문 분석 구분을 지킨다.
- 사용자 보고는 '버전 / 핵심 수정 / 이유 / 검증 결과 / 실제 환경 추가 확인 / 자동 작업 계속 여부'만 간결히 한다. 릴리스 안 했으면 기존 버전 기반 미배포임을 명시.
- 현재 GitHub에서 확인된 시작점은 main v0.15.28 (70113a5), 이전 독립 검수 후보는 work/audit-v01528이다. 이 값들은 과거 시작점일 뿐 매번 최신 상태를 다시 확인한다.
- 웹훅 설정 성공과 실제 다음 실행 성공을 구분한다. 일반 push(열린 PR 없음), 임의 GitHub 개발완료 이벤트, Work 종료 자체는 지원 트리거가 아니다. YES는 서버 필터가 아니라 실행 시 검사할 의미 조건이다. 플랫폼의 이벤트 전달·실행 제한은 준수하고 연속 실행을 보장한다고 말하지 않는다.
