'use strict';
(()=>{
  const VERSION='v0.3.2',DB='aram-rating-research-v03',STORE='kv',KEY='checkpoint-v03';
  const LOCK='__ARAM_RATING_PHASE_B3_V032_RUNNING__',CONFIRM='B3-DENSITY-FIRST-1000';
  const CFG=Object.freeze({phase:'B3',sampling_profile:'B3_DENSITY_FIRST_1000',max_expanded_players:50,max_matches_per_player:20,max_total_accepted_matches:1000,max_requests:150,request_timeout_ms:15000,retry_limit:2,cooldown_ms:1800,overall_timeout_ms:2100000,scan:100,history_limit:20,skip_headroom_lte:2,skip_duplicate_ratio_gte:.9,min_candidate_observations:2,min_resume_matches:500,dynamic_rerank:true});
  let runtime=null;const sleep=ms=>new Promise(r=>setTimeout(r,ms));
  function core(){const c=globalThis.ARAMRatingB3ManualCoreV032;if(!c)throw new Error('B3_core_not_loaded_run_preview_loader_first');return c}
  function openExisting(){return new Promise((res,rej)=>{const q=indexedDB.open(DB);let upgrading=false;q.onupgradeneeded=()=>{upgrading=true;try{q.transaction.abort()}catch{};rej(new Error('research_db_missing_refusing_to_create'))};q.onsuccess=()=>{if(upgrading)return;const db=q.result;if(!db.objectStoreNames.contains(STORE)){db.close();return rej(new Error('research_store_missing'))}res(db)};q.onerror=()=>{if(!upgrading)rej(q.error||new Error('research_db_open_failed'))}})}
  async function readCp(){const db=await openExisting();return new Promise((res,rej)=>{const tx=db.transaction(STORE,'readonly'),r=tx.objectStore(STORE).get(KEY);r.onsuccess=()=>{db.close();res(r.result??null)};r.onerror=()=>{db.close();rej(r.error)}})}
  async function writeCp(cp){const db=await openExisting();return new Promise((res,rej)=>{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).put(cp,KEY);tx.oncomplete=()=>{db.close();res()};tx.onerror=()=>{db.close();rej(tx.error)}})}
  function assertCp(cp,C){if(!cp||cp.schema!=='aram-rating-phase-b-checkpoint-v03')throw new Error('checkpoint_v03_required');const n=C.dedupeMatches(cp.matches||[]).length;if(n<500)throw new Error('b3_requires_existing_checkpoint_500_plus_matches_current_'+n);return cp}
  function summary(C,cp,label){const k=C.kpis(cp.matches||[]);return{label,checkpoint_matches:k.matches,players:k.players,single_match_fraction:k.single_match_fraction,players_2_plus:k.players_2_plus,players_5_plus:k.players_5_plus,players_10_plus:k.players_10_plus,connected_components:k.connected_components,giant_component_ratio:k.giant_component_ratio,completed_b3:(cp.completed_puuids_b3_v032||[]).length}}
  function timed(p,ms){let t;return Promise.race([Promise.resolve(p).finally(()=>clearTimeout(t)),new Promise((_,rej)=>{t=setTimeout(()=>rej(new Error('request_timeout')),ms)})])}
  async function request(opts){if(runtime?.abort)throw new Error('manual_abort');if(runtime.requests>=CFG.max_requests)throw new Error('max_request_count');runtime.requests++;return timed(window.aramDesktop.getAramMatchHistory(opts),CFG.request_timeout_ms)}
  async function preview(){const C=core(),cp=assertCp(await readCp(),C),k=C.kpis(cp.matches),top=C.buildCandidatePool(cp,CFG).slice(0,50);const out={status:'PREVIEW_ONLY',riot_lcu_collection_requests_performed:0,checkpoint_written:false,checkpoint_matches:k.matches,target_checkpoint_matches:1000,selected_count:top.length,density_first:true,candidates:top.map((x,i)=>({candidate:'Candidate '+String(i+1).padStart(2,'0'),source_pool:x.source_pool,observations:x.current_observation_count,expected_headroom:x.expected_new_headroom,network_overlap:x.existing_network_overlap,repeat_density_ratio:x.repeat_density_ratio,single_neighbor_recovery_ratio:x.single_neighbor_recovery_ratio,new_player_explosion_penalty:x.new_player_explosion_penalty,density_priority:x.b3_priority}))};console.table(out.candidates);console.log('[ARAM Rating v0.3.2] B3 PREVIEW ONLY',out);return out}
  async function runB3(options={}){
    if(options.confirm!==CONFIRM)throw new Error("explicit_confirmation_required: runB3({confirm:'B3-DENSITY-FIRST-1000'})");
    if(globalThis[LOCK])return{status:'already_running'};if(!window.aramDesktop?.getAramMatchHistory)throw new Error('desktop_match_history_bridge_unavailable');
    globalThis[LOCK]=true;runtime={abort:false,requests:0,started:Date.now()};
    try{const C=core(),cp=assertCp(await readCp(),C),before=summary(C,cp,'before_b3_v032');if(before.checkpoint_matches>=1000)return{status:'hard_cap_reached',before,after:before,delta:C.delta(before,before)};
      cp.phase='B3';cp.sampling_version=VERSION;cp.sampling_profile=CFG.sampling_profile;cp.completed_puuids_b3_v032=Array.isArray(cp.completed_puuids_b3_v032)?cp.completed_puuids_b3_v032:[];cp.expansion_logs_b3_v032=Array.isArray(cp.expansion_logs_b3_v032)?cp.expansion_logs_b3_v032:[];cp.b3_v032_before=cp.b3_v032_before||before;cp.status='b3_v032_collecting';await writeCp(cp);let expanded=0;
      while(cp.completed_puuids_b3_v032.length<50){if(runtime.abort){cp.status='manual_abort';break}if(Date.now()-runtime.started>CFG.overall_timeout_ms){cp.status='overall_timeout';break}if(C.kpis(cp.matches).matches>=1000){cp.status='hard_cap_reached';break}const pool=C.buildCandidatePool(cp,CFG);if(!pool.length){cp.status='candidate_pool_exhausted';break}const f=pool[0];let merged=null,err='',attempts=0;
        for(let a=0;a<=CFG.retry_limit;a++){attempts=a+1;try{const r=await request({limit:20,scan:100,target:{puuid:f.puuid},queueMode:'standard',priority:'background'});if(r?.connected===false)throw new Error('league_client_unavailable');const rows=C.dedupeMatches(r?.matches||[]).slice(0,20);merged=C.mergeExpansion(cp.matches,rows,f.puuid);if(!merged.valid.length)throw new Error('empty_target_history');if(!merged.target_hits)throw new Error('returned_history_does_not_match_target_puuid');break}catch(e){err=String(e?.message||e);if(a<CFG.retry_limit)await sleep(CFG.cooldown_ms*Math.pow(2,a))}}
        if(!merged){cp.expansion_logs_b3_v032.push({observations:f.current_observation_count,requests:attempts,new_unique_matches:0,error:err});cp.completed_puuids_b3_v032.push(f.puuid);await writeCp(cp);continue}
        const room=Math.max(0,1000-C.kpis(cp.matches).matches),accepted=merged.new_matches.slice(0,room),m=C.mergeExpansion(cp.matches,accepted,f.puuid);cp.matches=C.dedupeMatches([...cp.matches,...accepted]);cp.completed_puuids_b3_v032.push(f.puuid);cp.expansion_logs_b3_v032.push({observations:f.current_observation_count,requests:attempts,new_unique_matches:accepted.length,duplicates:merged.duplicates.length,new_players:m.new_players,known_player_appearances:m.already_known_player_appearances,threshold_crossings:m.threshold_crossings,density_gain:m.density_gain,information_gain:m.information_gain});expanded++;await writeCp(cp);const now=C.kpis(cp.matches);console.log(`[ARAM Rating v0.3.2] B3 ${cp.completed_puuids_b3_v032.length}/50 · +${accepted.length} · accepted ${now.matches}/1000 · 2+ ${now.players_2_plus} · 5+ ${now.players_5_plus} · 10+ ${now.players_10_plus} · single ${(100*now.single_match_fraction).toFixed(2)}%`);if(now.matches>=1000){cp.status='hard_cap_reached';break}await sleep(CFG.cooldown_ms)}
      if(cp.status==='b3_v032_collecting')cp.status=cp.completed_puuids_b3_v032.length>=50?'phase_complete':'paused';const after=summary(C,cp,'after_b3_v032');cp.b3_v032_final=after;cp.b3_v032_delta=C.delta(cp.b3_v032_before,after);await writeCp(cp);const out={status:cp.status,expanded_this_run:expanded,requests_this_run:runtime.requests,before:cp.b3_v032_before,after,delta:cp.b3_v032_delta,privacy:{contains_raw_puuid:false,contains_riot_id:false},production_changed:false};console.log('[ARAM Rating v0.3.2] B3 stopped',out);return out
    }finally{runtime=null;delete globalThis[LOCK]}}
  function abort(){if(runtime)runtime.abort=true;return!!runtime}
  async function status(){const C=core(),cp=assertCp(await readCp(),C);return{...summary(C,cp,'current'),status:cp.status||null,automatic_collection:false}}
  window.aramRatingB3V032={version:VERSION,config:{...CFG},preview,runB3,abort,status,confirmation:CONFIRM};console.log('[ARAM Rating v0.3.2] B3 manual helper ready. NO Riot/LCU collection request has started.');
})();
