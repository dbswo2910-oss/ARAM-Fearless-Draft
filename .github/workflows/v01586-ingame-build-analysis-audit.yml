name: v0.15.86 IN GAME Build Analysis Audit

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run IN GAME build-analysis audit
        run: node tools/ingame-build-analysis-v01586-audit.js
      - name: Upload audit report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ingame-build-analysis-v01586-report
          path: audit-output/ingame-build-analysis-v01586-report.json
          if-no-files-found: error
