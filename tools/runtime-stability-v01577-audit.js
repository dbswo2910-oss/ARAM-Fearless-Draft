'use strict';
const fs=require('fs'),path=require('path');
const ROOT=path.resolve(__dirname,'..');
const read=p=>fs.readFileSync(path.join(ROOT,p),'utf8'),exists=p=>fs.existsSync(path.join(ROOT,p));
const m=JSON.parse(read('update/manifest.json')),by=new Map((m.files||[]).map(x=>[x.path,x.source]));
const checks=[];const ok=(n,p,d='')=>checks.push({name:n,pass:!!p,detail:d});
const ge=(a,b)=>{const A=String(a||'0').split('.').map(Number),B=String(b||'0').split('.').map(Number),n=Math.max(A.length,B.length);for(let i=0;i<n;i++){if((A[i]||0)!==(B[i]||0))return(A[i]||0)>(B[i]||0)}return true};
const src=t=>by.get(t)&&exists(by.get(t))?read(by.get(t)):'';
const pkg=JSON.parse(src('package.json')||'{}'),entryTarget=String(pkg.main||''),entry=src(entryTarget),v79Entry=src('main-v01579.js'),preload=src('preload.js');
function collectMainChain(startTarget){
  const out=[],seen=new Set();let target=startTarget;
  while(target&&by.get(target)&&!seen.has(target)&&out.length<40){
    seen.add(target);const source=by.get(target);if(!source||!exists(source))break;const text=read(source);out.push({target,source,text});
    const refs=[...text.matchAll(/['"](main-v\d+\.js)['"]/g)].map(x=>x[1]);
    target=refs.find(x=>by.has(x)&&!seen.has(x))||'';
  }
  return out;
}
const entryChain=collectMainChain(entryTarget),entryChainTargets=entryChain.map(x=>x.target);
const bb=src('freeze-blackbox-v01577.js'),ren=src('runtime-blackbox-v01577.js'),stab=src('runtime-source-stability-v01577.js'),loader77=src('runtime-loader-v01577.js');
const currentLoaderTarget=by.get('runtime-loader-v01579.js')?'runtime-loader-v01579.js':'runtime-loader-v01577.js',currentLoader=src(currentLoaderTarget);
const historicalEntry=exists('update/v0.15.77/main-v01577.js')?read('update/v0.15.77/main-v01577.js'):'';
for(const [n,s] of [['current entry',entry],['v0.15.79 safety successor base',v79Entry],['current preload',preload],['blackbox',bb],['renderer blackbox',ren],['v77 source stability',stab],['v77 isolated loader',loader77],['current loader',currentLoader],['historical v77 entry',historicalEntry]]){try{new Function(s);ok(n+' parses',true)}catch(e){ok(n+' parses',false,e.message)}}
ok('manifest is v0.15.77 or newer',ge(m.version,'0.15.77'),m.version);ok('package is v0.15.77 or newer',ge(pkg.version,'0.15.77'),pkg.version||'');ok('current package entry delivered',!!entryTarget&&!!entry,entryTarget);ok('historical v0.15.77 entry remains in repository',!!historicalEntry);
for(const t of ['freeze-blackbox-v01577.js','runtime-blackbox-v01577.js','runtime-source-stability-v01577.js','runtime-loader-v01577.js'])ok('v77 infrastructure still delivered '+t,!!by.get(t)&&!!src(t),by.get(t)||'');
ok('current preload preserves blackbox trace',preload.includes("traceDiagnostic: payload => ipcRenderer.send('diagnostics:blackbox-v01577'"));ok('current preload preserves last diagnostic API',preload.includes("getLastDiagnostic: () => ipcRenderer.invoke('diagnostics:get-last-v01577')"));
ok('current entry inherits v0.15.79 safety successor',entryChainTargets.includes('main-v01579.js'),entryChainTargets.join(' -> '));
ok('successor base installs v77 blackbox before core',v79Entry.includes("require('./freeze-blackbox-v01577')")&&v79Entry.indexOf('blackbox.install')>0&&v79Entry.indexOf('blackbox.install')<v79Entry.indexOf("const autosyncCore=require('./autosync-core')"));
const blackboxPos=v79Entry.indexOf("'runtime-blackbox-v01577.js'"),perfPos=blackboxPos>=0?v79Entry.indexOf("'runtime-performance-v01568.js'",blackboxPos):-1;ok('renderer blackbox remains first diagnostic runtime',blackboxPos>=0&&perfPos>blackboxPos,`${blackboxPos}/${perfPos}`);
const direct77=v79Entry.includes("runtime-loader-v01577').injectRuntimeStack"),wrapped79=v79Entry.includes("runtime-loader-v01579').injectRuntimeStack")&&currentLoader.includes("require('./runtime-loader-v01577')")&&currentLoader.includes('prior.injectRuntimeStack');ok('current loader still delegates to v77 isolated injector',direct77||wrapped79,currentLoaderTarget);
ok('v77 loader isolates per-file failures',loader77.includes('runtime-inject-error')&&loader77.includes('for(let i=0;i<scripts.length;i++)')&&loader77.includes('results.push({file,code:injCode,ok})'));ok('v77 loader always finalizes stability owners',loader77.includes('runtime-finalize-end')&&loader77.includes('finishBootstrap')&&loader77.includes('restoreTimerHook')&&loader77.includes('aramBlackboxV01577?.rescan'));ok('v77 loader keeps deterministic RTI codes',loader77.includes("'RTI-'+String(i+1).padStart(3,'0')")&&loader77.includes("'RTI-FIN'"));
ok('watchdog probe remains single-flight',bb.includes('if(wc.isDestroyed()||probeBusy)return')&&!bb.includes('Promise.race([wc.executeJavaScript'));ok('debugger remains pre-armed',bb.indexOf('prepareDebugger().catch(()=>{})')>0&&bb.indexOf('prepareDebugger().catch(()=>{})')<bb.indexOf('const captureStack=async'));ok('FRZ-003 stack persistence remains',bb.includes("recordStack('FRZ-003'")&&bb.includes('functionName')&&bb.includes('line:Number')&&bb.includes('column:Number'));ok('renderer JS/long-task codes remain',ren.includes("'JS-E001'")&&ren.includes("'JS-E002'")&&ren.includes("'JS-P001'")&&ren.includes("'LT-001'")&&ren.includes("'LT-002'"));
(async()=>{let tmp=[];try{const loaderPath=path.join(ROOT,by.get('runtime-loader-v01577.js')),dir=path.dirname(loaderPath);tmp=['__audit77_ok_a.js','__audit77_fail.js','__audit77_ok_b.js'];fs.writeFileSync(path.join(dir,tmp[0]),'window.A=1;');fs.writeFileSync(path.join(dir,tmp[1]),"throw new Error('audit fail');");fs.writeFileSync(path.join(dir,tmp[2]),'window.B=1;');const {injectRuntimeStack}=require(loaderPath),calls=[],events=[];const wc={executeJavaScript:code=>{calls.push(code);return code.includes('sourceURL=__audit77_fail.js')?Promise.reject(new Error('audit fail')):Promise.resolve(true)}};const fake={record:(code,kind,detail)=>events.push({code,kind,detail}),captureNow:()=>Promise.resolve(true)};const r=await injectRuntimeStack({mainWindow:{webContents:wc},scripts:tmp,blackbox:fake,patchRuntimeSource:(_f,c)=>c});ok('runtime failure still does not abort later injection',r.map(x=>x.ok).join(',')==='true,false,true'&&calls.some(x=>x.includes('sourceURL=__audit77_ok_b.js')));ok('runtime failure still reaches finalizer',events.some(x=>x.code==='RTI-FIN'&&x.kind==='runtime-finalize-end')&&calls.some(x=>x.includes('restoreTimerHook')))}catch(e){ok('runtime failure still does not abort later injection',false,e.message);ok('runtime failure still reaches finalizer',false,e.message)}finally{try{const dir=path.dirname(path.join(ROOT,by.get('runtime-loader-v01577.js')));for(const f of tmp)fs.rmSync(path.join(dir,f),{force:true})}catch{}}
ok('v77 infrastructure remains score neutral',!entry.includes('teamScore(')&&!entry.includes('recommendPicks(')&&!entry.includes('recommendBans(')&&ren.includes('score_logic_changed:false'));const report={version:'0.15.77-historical',checks,pass:checks.every(x=>x.pass),score_logic_changed:false,info:{forwardCompatible:true,preserves:'pre-armed blackbox + fault-isolated v77 injector even when newer safety wrappers are present',entryChain:entryChainTargets}};fs.mkdirSync(path.join(ROOT,'audit-output'),{recursive:true});fs.writeFileSync(path.join(ROOT,'audit-output/runtime-stability-v01577-report.json'),JSON.stringify(report,null,2));for(const c of checks)console.log(`${c.pass?'PASS':'FAIL'} ${c.name}${c.detail?' · '+c.detail:''}`);if(!report.pass)process.exit(1)})();
