'use strict';
const fs=require('fs'),path=require('path');
const ROOT=path.resolve(__dirname,'../..'),R=path.join(ROOT,'research/aram-rating-v032');
let pass=0;const ok=(c,m)=>{if(!c)throw new Error('B5.1 R11 AUDIT: '+m);pass++};
const p=path.join(R,'phase-b51r11-v032-evaluate-devtools.js'),s=fs.readFileSync(p,'utf8');new Function(s);
ok(s.includes("PHASE='B5.1-R11'"),'phase');
ok(s.includes('checkpoint_matches:1984'),'exact post-R10 match precondition');
ok(s.includes('completed:4'),'exact post-R10 completed precondition');
ok(s.includes('remaining:11'),'exact post-R10 remaining precondition');
ok(s.includes("last_recovery_phase:'B5.1-R10'"),'requires R10 recovery state');
ok(s.includes("primary_metric:'log_loss'"),'accuracy-first primary metric');
ok(s.includes("secondary_metrics:['brier','ece','accuracy']"),'secondary metrics');
ok(s.includes("ENGINE_COMMIT='b114d21019872326bdb2cf410772fb0c1ad8ce34'"),'immutable reference-engine commit');
ok(s.includes('buildLatestRun'),'reference model evaluation');
ok(s.includes('candidate_winner'),'candidate gate classification');
ok(s.includes('marginal_yield_low'),'collection saturation signal');
ok(s.includes('new_unique_per_request'),'recovery efficiency signal');
ok(s.includes('observed_leader'),'observed model leader reported');
ok(s.includes('selection:run.selection'),'full selection gate reported');
ok(s.includes('raw_matches_returned:false'),'no raw match export');
ok(s.includes('raw_puuid_returned:false'),'no raw PUUID export');
ok(s.includes('riot_lcu_requests:0'),'zero Riot/LCU contract');
ok(!s.includes('getAramMatchHistory'),'no live history bridge');
ok(!s.includes("'readwrite'")&&!s.includes('.put('),'no checkpoint writes');
ok(!s.includes('deleteDatabase(')&&!s.includes('localStorage.clear('),'no destructive reset');
ok(!/\.evaluate\(\)/.test(s.split('console.log(')[0].slice(-200)),'no automatic evaluate');
const manifest=JSON.parse(fs.readFileSync(path.join(ROOT,'update/manifest.json'),'utf8'));
ok(!(manifest.files||[]).some(x=>String(x.source||'').includes('phase-b51r11-v032-evaluate-devtools')),'R11 not shipped');
const report={status:'SUCCESS',passes:pass,phase:'B5.1-R11',checkpoint_matches:1984,completed:4,remaining:11,riot_lcu_requests:0,checkpoint_written:false,production_changed:false,automatic_run:false,primary_metric:'log_loss'};
fs.mkdirSync(path.join(ROOT,'audit-output'),{recursive:true});fs.writeFileSync(path.join(ROOT,'audit-output/aram-rating-v032-b51r11-evaluator-audit.json'),JSON.stringify(report,null,2)+'\n');
console.log(`B5.1 R11 READ-ONLY EVALUATOR AUDIT: SUCCESS · ${pass} checks`);
