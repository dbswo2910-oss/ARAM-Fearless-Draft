'use strict';
const fs=require('fs'),path=require('path');
const ROOT=path.resolve(__dirname,'../..'),P=path.join(ROOT,'research/aram-rating-v032/phase-b51r7-v032-devtools.js');
let pass=0;const ok=(c,m)=>{if(!c)throw new Error('B5.1 R7 AUDIT: '+m);pass++};
const s=fs.readFileSync(P,'utf8');new Function(s);
ok(s.includes("PHASE='B5.1-R7'"),'phase tag');
ok(s.includes('READ_ONLY_REPAIR_PREVIEW'),'read-only repair preview mode');
ok(s.includes('riot_lcu_requests:0'),'zero Riot/LCU requests contract');
ok(s.includes('checkpoint_written:false'),'no checkpoint writes contract');
ok(s.includes('storage_mutation:false'),'no storage mutation contract');
ok(s.includes('REPAIR_PREVIEW_15_HTTP400_FALSE_COMPLETIONS'),'15-anchor forensic classification');
ok(s.includes("errorClass(x?.error)==='HTTP_400'")&&s.includes('new_unique_matches||0'),'repair preview limited to HTTP400 zero-yield logs');
ok(s.includes('would_change_matches:false'),'match data is never changed');
ok(s.includes('match_data_integrity'),'match fingerprint verification');
ok(!s.includes('getAramMatchHistory'),'no live bridge call');
ok(!s.includes("transaction(STORE,'readwrite')"),'no readwrite transaction');
ok(!s.includes('.put('),'no IndexedDB write');
ok(!s.includes('deleteDatabase(')&&!s.includes('localStorage.clear('),'no destructive reset');
const report={status:'SUCCESS',passes:pass,phase:'B5.1-R7',mode:'READ_ONLY_REPAIR_PREVIEW',riot_lcu_requests:0,checkpoint_written:false,storage_mutation:false,production_changed:false};
fs.mkdirSync(path.join(ROOT,'audit-output'),{recursive:true});fs.writeFileSync(path.join(ROOT,'audit-output/aram-rating-v032-b51r7-repair-preview-audit.json'),JSON.stringify(report,null,2)+'\n');console.log(`B5.1 R7 REPAIR PREVIEW AUDIT: SUCCESS · ${pass} checks`);
