'use strict';
const fs=require('fs'),path=require('path'),crypto=require('crypto');
const ROOT=path.resolve(__dirname,'../..');
const OUT=path.join(ROOT,'audit-output','r17-installed-shadow-rc-kit');
const MODULES=Object.freeze([
  ['storage','src/research/storage.js'],
  ['rating-engine','src/research/rating-engine.js'],
  ['dual-shadow','src/research/dual-shadow.js'],
  ['shadow-promotion-gate','src/research/shadow-promotion-gate.js'],
  ['shadow-evidence-store','src/research/shadow-evidence-store.js'],
  ['installed-shadow-rc','src/research/installed-shadow-rc.js']
]);
const sha=b=>crypto.createHash('sha256').update(b).digest('hex');
function read(rel){return fs.readFileSync(path.join(ROOT,rel),'utf8')}
function ensure(d){fs.mkdirSync(d,{recursive:true})}
function write(name,body){const p=path.join(OUT,name);ensure(path.dirname(p));fs.writeFileSync(p,body,'utf8');return{file:name,sha256:sha(Buffer.from(body,'utf8')),bytes:Buffer.byteLength(body)}}
function buildRenderer(){
  const entries=MODULES.map(([id,rel])=>`${JSON.stringify(id)}:function(module,exports,require){\n${read(rel)}\n}`).join(',\n');
  return `(function(){\n'use strict';\nconst __mods={${entries}};\nconst __cache={};\nfunction __norm(id){return String(id||'').replace(/^\\.\\//,'').replace(/\\.js$/,'')}\nfunction __req(id){id=__norm(id);if(__cache[id])return __cache[id].exports;const fn=__mods[id];if(!fn)throw new Error('R17 shadow module unavailable: '+id);const module={exports:{}};__cache[id]=module;fn(module,module.exports,__req);return module.exports}\nconst MARKER='[ARAM_R17_SHADOW_RC_RESULT]';\nfunction publish(result){try{Object.defineProperty(window,'__ARAM_R17_SHADOW_RC__',{value:Object.freeze(result),configurable:true})}catch{window.__ARAM_R17_SHADOW_RC__=result}try{console.log(MARKER+JSON.stringify(result))}catch{}return result}\nfunction run(){const rc=__req('installed-shadow-rc');return Promise.resolve(rc.runOnce({windowRef:window})).then(publish).catch(e=>publish({schema:'aram-rating-installed-shadow-rc-v1',state:'error',error_name:e&&e.name||'Error',production_active:false,production_score_changed:false,ui_changed:false,canonical_checkpoint_written:false,automatic_promotion:false,production_activation_authorized:false,privacy:{raw_puuid_returned:false,raw_match_id_returned:false,identity_mapping_returned:false}}))}\nif(typeof window.requestIdleCallback==='function')window.requestIdleCallback(()=>{void run()},{timeout:3000});else Promise.resolve().then(()=>run());\nreturn{installed:true,mode:'r17-shadow-rc',production_active:false};\n})()\n//# sourceURL=aram-rating-r17-shadow-rc.js\n`;
}
function buildMain(){return `'use strict';\nconst fs=require('fs'),path=require('path');\nconst {app}=require('electron');\nconst MARKER='[ARAM_R17_SHADOW_RC_RESULT]';\nconst renderer=fs.readFileSync(path.join(__dirname,'r17-shadow-renderer.js'),'utf8');\nconst reportPath=String(process.env.ARAM_R17_SHADOW_RC_REPORT||'').trim();\nfunction writeReport(result){if(!reportPath)return;try{fs.mkdirSync(path.dirname(reportPath),{recursive:true});fs.writeFileSync(reportPath,JSON.stringify({status:result&&result.state==='available'?'SUCCESS':'FAILURE',stage:'R17_PHYSICAL_INSTALLED_SHADOW_RC',captured_at:new Date().toISOString(),result},null,2)+'\\n','utf8')}catch(e){try{console.warn('[R17 shadow RC] report write failed:',e&&e.message||String(e))}catch{}}}\napp.on('browser-window-created',(_event,win)=>{\n  try{win.webContents.on('console-message',(_e,...args)=>{const msg=args.find(x=>typeof x==='string'&&x.startsWith(MARKER));if(!msg)return;try{const result=JSON.parse(msg.slice(MARKER.length));writeReport(result);console.log('[R17 shadow RC] result captured',{state:result.state,history_requests:result.history_requests,augmented_matches:result.augmented_matches,promotion_status:result.promotion_status})}catch(e){console.warn('[R17 shadow RC] result parse failed:',e&&e.message||String(e))}})}catch{}\n  const inject=()=>{try{if(!win||win.isDestroyed?.()||win.webContents?.isDestroyed?.())return;Promise.resolve(win.webContents.executeJavaScript(renderer,false)).catch(e=>console.warn('[R17 shadow RC] renderer inject failed:',e&&e.message||String(e)))}catch(e){console.warn('[R17 shadow RC] renderer inject failed:',e&&e.message||String(e))}};\n  try{win.webContents.on('did-finish-load',inject)}catch(e){console.warn('[R17 shadow RC] hook failed:',e&&e.message||String(e))}\n});\nrequire('./main-v0160.js');\n`}
function buildPowerShell(){return `param(\n  [string]$AppDir = '',\n  [string]$ElectronExe = '',\n  [string]$ReportDir = ''\n)\n$ErrorActionPreference='Stop'\nSet-StrictMode -Version Latest\n$KitDir=$PSScriptRoot\nif(-not $ReportDir){$ReportDir=Join-Path $KitDir 'physical-output'}\nNew-Item -ItemType Directory -Force -Path $ReportDir|Out-Null\nfunction Read-Version([string]$d){try{return [string]((Get-Content (Join-Path $d 'package.json') -Raw|ConvertFrom-Json).version}catch{return ''}}\nfunction Find-AppDir{\n  $roots=@((Join-Path $env:LOCALAPPDATA 'ARAM Fearless Draft AutoUpdate\\appfiles'),(Join-Path $env:LOCALAPPDATA 'ARAM Fearless Draft AutoUpdater\\appfiles'))\n  Get-ChildItem $env:LOCALAPPDATA -Directory -ErrorAction SilentlyContinue|Where-Object{$_.Name -like 'ARAM Fearless Draft AutoUpdate*'}|ForEach-Object{$roots+=(Join-Path $_.FullName 'appfiles')}\n  $c=@();foreach($r in ($roots|Select-Object -Unique)){if((Test-Path (Join-Path $r 'package.json')) -and (Test-Path (Join-Path $r 'index.html'))){try{$v=[version](Read-Version $r);$c+=[pscustomobject]@{Dir=$r;Version=$v}}catch{}}};return ($c|Sort-Object Version -Descending|Select-Object -First 1).Dir\n}\nfunction Find-Electron{\n  $roots=@((Join-Path $env:LOCALAPPDATA 'ARAM Fearless Draft AutoUpdate'),(Join-Path $env:LOCALAPPDATA 'ARAM Fearless Draft AutoUpdater'),(Join-Path $env:LOCALAPPDATA 'ARAM Fearless Draft'))\n  foreach($r in $roots){if(Test-Path $r){$hit=Get-ChildItem $r -Recurse -Filter electron.exe -File -ErrorAction SilentlyContinue|Select-Object -First 1;if($hit){return $hit.FullName}}};return ''\n}\nfunction Electron-Pids([string]$exe){$target=[IO.Path]::GetFullPath($exe);$ids=@();Get-CimInstance Win32_Process -Filter "Name='electron.exe'" -ErrorAction SilentlyContinue|ForEach-Object{if($_.ExecutablePath){try{if([string]::Equals([IO.Path]::GetFullPath([string]$_.ExecutablePath),$target,[StringComparison]::OrdinalIgnoreCase)){$ids+=[int]$_.ProcessId}}catch{}}};return @($ids|Sort-Object -Unique)}\nfunction Stop-Pids([int[]]$ids){foreach($id in @($ids|Sort-Object -Unique)){try{Stop-Process -Id $id -Force -ErrorAction SilentlyContinue}catch{}}}\nfunction Quote([string]$v){return '"'+$v.Replace('"','\\"')+'"'}\nfunction Probe([string]$tag,[string]$app,[string]$electron,[string]$userData){$out=Join-Path $ReportDir "state-$tag.json";$args=('{0} --app-dir {1} --user-data {2} --report {3}' -f (Quote (Join-Path $KitDir 'windows-real-state-probe.js')),(Quote $app),(Quote $userData),(Quote $out));$p=Start-Process -FilePath $electron -ArgumentList $args -Wait -PassThru;if($p.ExitCode -ne 0 -or -not(Test-Path $out)){throw "state probe $tag failed"};return Get-Content $out -Raw|ConvertFrom-Json}\nif($env:OS -ne 'Windows_NT'){throw 'R17 physical RC must run on Windows'}\nif(-not $AppDir){$AppDir=Find-AppDir};if(-not $AppDir){throw 'installed ARAM appfiles not found; pass -AppDir'}\nif((Read-Version $AppDir) -ne '0.16.0'){throw "R17 RC expects installed v0.16.0, found $(Read-Version $AppDir)"}\nif(-not $ElectronExe){$ElectronExe=Find-Electron};if(-not $ElectronExe -or -not(Test-Path $ElectronExe)){throw 'Electron runtime not found; pass -ElectronExe'}\n$userData=Join-Path ([Environment]::GetFolderPath([Environment+SpecialFolder]::ApplicationData)) 'aram-fearless-draft';if(-not(Test-Path $userData)){throw 'stable userData aram-fearless-draft is missing'}\n$tempRoot=Join-Path $env:TEMP ('aram-r17-shadow-rc-'+$PID);$tempApp=Join-Path $tempRoot 'appfiles';if(Test-Path $tempRoot){Remove-Item $tempRoot -Recurse -Force};New-Item -ItemType Directory -Force -Path $tempApp|Out-Null\nCopy-Item (Join-Path $AppDir '*') $tempApp -Recurse -Force\nCopy-Item (Join-Path $KitDir 'main-r17-shadow-rc.js') $tempApp -Force;Copy-Item (Join-Path $KitDir 'r17-shadow-renderer.js') $tempApp -Force\n$pkgPath=Join-Path $tempApp 'package.json';$pkg=Get-Content $pkgPath -Raw|ConvertFrom-Json;$pkg.main='main-r17-shadow-rc.js';$pkg|ConvertTo-Json -Depth 8|Set-Content $pkgPath -Encoding UTF8\n$pre=Probe 'before' $tempApp $ElectronExe $userData\n$shadowReport=Join-Path $ReportDir 'r17-shadow-result.json';if(Test-Path $shadowReport){Remove-Item $shadowReport -Force}\n$baseline=@(Electron-Pids $ElectronExe);$oldEnv=$env:ARAM_R17_SHADOW_RC_REPORT;$env:ARAM_R17_SHADOW_RC_REPORT=$shadowReport\ntry{$p=Start-Process -FilePath $ElectronExe -ArgumentList (Quote $tempApp) -PassThru;$deadline=(Get-Date).AddSeconds(60);while((Get-Date)-lt $deadline -and -not(Test-Path $shadowReport)){Start-Sleep -Milliseconds 500}}finally{if($null -eq $oldEnv){Remove-Item Env:ARAM_R17_SHADOW_RC_REPORT -ErrorAction SilentlyContinue}else{$env:ARAM_R17_SHADOW_RC_REPORT=$oldEnv}}\n$after=@(Electron-Pids $ElectronExe);$new=@($after|Where-Object{$baseline -notcontains $_});Stop-Pids $new;try{if($p -and -not $p.HasExited){Stop-Process -Id $p.Id -Force -ErrorAction SilentlyContinue}}catch{};Start-Sleep -Seconds 1\nif(-not(Test-Path $shadowReport)){throw 'R17 shadow result was not produced within 60 seconds'}\n$post=Probe 'after' $tempApp $ElectronExe $userData;$shadow=Get-Content $shadowReport -Raw|ConvertFrom-Json;$r=$shadow.result\n$checkpointStable=([string]$pre.research_checkpoint_sha256 -eq [string]$post.research_checkpoint_sha256)\n$success=($shadow.status -eq 'SUCCESS' -and $r.state -eq 'available' -and [int]$r.history_requests -le 1 -and -not [bool]$r.production_active -and -not [bool]$r.production_score_changed -and -not [bool]$r.canonical_checkpoint_written -and -not [bool]$r.ui_changed -and -not [bool]$r.production_activation_authorized -and $checkpointStable -and [int]$r.baseline_matches -ge 2020 -and -not [bool]$r.privacy.raw_puuid_returned -and -not [bool]$r.privacy.raw_match_id_returned)\n$summary=[ordered]@{status=if($success){'SUCCESS'}else{'FAILURE'};stage='R17_TEMP_COPY_PHYSICAL_ACCEPTANCE';privacy_safe=$true;production_install_mutated=$false;temp_copy_used=$true;production_version='0.16.0';canonical_checkpoint_stable=$checkpointStable;before_matches=[int]$pre.research_checkpoint_matches;after_matches=[int]$post.research_checkpoint_matches;shadow=[ordered]@{state=$r.state;history_requests=[int]$r.history_requests;history_error=$r.history_error;baseline_matches=[int]$r.baseline_matches;stored_delta_matches=[int]$r.stored_delta_matches;augmented_matches=[int]$r.augmented_matches;evidence_snapshots=[int]$r.evidence_snapshots;observed_leader=$r.observed_leader;observed_runner_up=$r.observed_runner_up;selection_status=$r.selection_status;promotion_status=$r.promotion_status;production_activation_authorized=$false}}\n$summary|ConvertTo-Json -Depth 8|Set-Content (Join-Path $ReportDir 'r17-physical-acceptance.json') -Encoding UTF8\ntry{Remove-Item $tempRoot -Recurse -Force -ErrorAction SilentlyContinue}catch{}\nWrite-Host "R17 TEMP-COPY PHYSICAL ACCEPTANCE: $($summary.status) -> $(Join-Path $ReportDir 'r17-physical-acceptance.json')"\nif(-not $success){exit 1}\n`}
function build(){
  fs.rmSync(OUT,{recursive:true,force:true});ensure(OUT);
  const files=[];files.push(write('r17-shadow-renderer.js',buildRenderer()));files.push(write('main-r17-shadow-rc.js',buildMain()));files.push(write('run-r17-shadow-rc.ps1',buildPowerShell()));files.push(write('windows-real-state-probe.js',read('tools/stability/windows-real-state-probe.js')));
  const manifest={schema:'aram-rating-r17-installed-shadow-rc-kit-v1',production_version:'0.16.0',mode:'temporary_copy_only',production_install_mutated:false,production_manifest_mutated:false,user_data_identity:'aram-fearless-draft',canonical_research_db:'aram-rating-research-v03',shadow_evidence_db:'aram-rating-shadow-evidence-v1',files,source_modules:MODULES.map(([id,rel])=>({id,rel,sha256:sha(Buffer.from(read(rel),'utf8'))}))};
  files.push(write('rc-kit.json',JSON.stringify(manifest,null,2)+'\n'));return{...manifest,files};
}
if(require.main===module){const report=build();console.log('R18 INSTALLED SHADOW RC KIT: BUILT',JSON.stringify({files:report.files.length,mode:report.mode,production_manifest_mutated:false}))}
module.exports={ROOT,OUT,MODULES,buildRenderer,buildMain,buildPowerShell,build};
