'use strict';
const fs=require('fs'),path=require('path'),crypto=require('crypto');
const ROOT=path.resolve(__dirname,'../..');
const OUT=path.join(ROOT,'audit-output','universal-rating-r1-shadow-rc-kit');
const SOURCES=Object.freeze([
  ['src/rating/universal/contracts.js','universal-shadow/src/rating/universal/contracts.js'],
  ['src/rating/universal/normalizer.js','universal-shadow/src/rating/universal/normalizer.js'],
  ['src/rating/universal/confidence.js','universal-shadow/src/rating/universal/confidence.js'],
  ['src/rating/universal/estimator.js','universal-shadow/src/rating/universal/estimator.js'],
  ['src/rating/universal/store.js','universal-shadow/src/rating/universal/store.js'],
  ['src/rating/universal/service.js','universal-shadow/src/rating/universal/service.js'],
  ['src/rating/universal/runtime.js','universal-shadow/src/rating/universal/runtime.js'],
  ['src/rating/universal/index.js','universal-shadow/src/rating/universal/index.js'],
  ['update/v0.15.129/rating-engine-v01.js','universal-shadow/update/v0.15.129/rating-engine-v01.js']
]);
const sha=b=>crypto.createHash('sha256').update(b).digest('hex');
const read=rel=>fs.readFileSync(path.join(ROOT,rel),'utf8');
const ensure=d=>fs.mkdirSync(d,{recursive:true});
function write(rel,body){const p=path.join(OUT,rel);ensure(path.dirname(p));fs.writeFileSync(p,body,'utf8');return{file:rel,sha256:sha(Buffer.from(body,'utf8')),bytes:Buffer.byteLength(body)}}
function buildMain(){return `'use strict';\nconst fs=require('fs'),path=require('path');\nconst {app}=require('electron');\nconst {createUniversalRatingRuntime}=require('./universal-shadow/src/rating/universal/runtime');\nconst reportPath=String(process.env.ARAM_UNIVERSAL_R1_REPORT||'').trim();\nconst dbRoot=String(process.env.ARAM_UNIVERSAL_R1_DB_ROOT||'').trim();\nif(!dbRoot)throw new Error('ARAM_UNIVERSAL_R1_DB_ROOT required');\nconst runtime=createUniversalRatingRuntime({userDataPath:dbRoot});\nlet reportWritten=false,started=false;\nfunction quit(){setTimeout(()=>{try{app.quit()}catch{}},300)}\nfunction writeReport(result){if(reportWritten)return;reportWritten=true;const envelope={status:result&&result.state==='available'?'SUCCESS':'FAILURE',stage:'UNIVERSAL_RATING_R1_PHYSICAL_SHADOW_RC',captured_at:new Date().toISOString(),result};if(reportPath){fs.mkdirSync(path.dirname(reportPath),{recursive:true});fs.writeFileSync(reportPath,JSON.stringify(envelope,null,2)+'\\n','utf8')}try{console.log('[UNIVERSAL_RATING_R1_RESULT]'+JSON.stringify(result))}catch{}quit()}\nfunction fail(e,extra={}){writeReport({schema:'aram-universal-rating-physical-shadow-v1',state:'error',error_name:e&&e.name||'Error',error_message:String(e&&e.message||e||'unknown_error').slice(0,240),history_requests:Number(extra.history_requests)||0,production_active:false,automatic_promotion:false,production_rating:null,privacy:{raw_puuid_returned:false,raw_match_id_returned:false}})}\nasync function run(win){if(started)return;started=true;let resolved;try{resolved=await win.webContents.executeJavaScript(\`(async()=>{const b=window.aramDesktop;if(!b||typeof b.getAramMatchHistory!=='function')return{ok:false,reason:'match_history_bridge_unavailable',history_requests:0};const first=await b.getAramMatchHistory({limit:1,scan:20,target:{current:true},queueMode:'standard',priority:'interactive'});if(!first||first.connected===false||!Array.isArray(first.matches)||!first.matches.length)return{ok:false,reason:'current_history_unavailable',history_requests:1};const roots=g=>[g,g&&g.game,g&&g.match,g&&g.data,g&&g.raw,g&&g.info].filter(Boolean);const ps=g=>{for(const r of roots(g)){if(Array.isArray(r&&r.participants)&&r.participants.length)return r.participants;const a=Array.isArray(r&&r.team)?r.team:[],c=Array.isArray(r&&r.enemy)?r.enemy:[];if(a.length||c.length)return a.concat(c)}return[]};const id=p=>String((p&&p.puuid)||(p&&p.player&&p.player.puuid)||'').trim();const local=String((first.localAccount&&first.localAccount.puuid)||(first.account&&first.account.puuid)||'').trim();const peer=ps(first.matches[0]).map(id).find(x=>x&&x!==local);if(!peer)return{ok:false,reason:'peer_puuid_not_found',history_requests:1};const second=await b.getAramMatchHistory({limit:20,scan:100,target:{puuid:peer},queueMode:'standard',priority:'background'});if(!second||second.connected===false)return{ok:false,reason:'searched_peer_history_unavailable',history_requests:2};return{ok:true,history_requests:2,player:{puuid:peer,gameName:String(second.account&&second.account.gameName||''),tagLine:String(second.account&&second.account.tagLine||''),platformId:'KR'},matches:Array.isArray(second.matches)?second.matches:[]};})()\`,false)}catch(e){return fail(e,{history_requests:0})}if(!resolved||resolved.ok!==true)return fail(new Error(resolved&&resolved.reason||'renderer_probe_failed'),{history_requests:resolved&&resolved.history_requests});try{const rating=await runtime.rateResolved({player:resolved.player,matches:resolved.matches,reason:'PHYSICAL_SHADOW_SEARCHED_PEER'});const elo=rating&&rating.candidates&&rating.candidates.elo,glicko=rating&&rating.candidates&&rating.candidates.glicko;const good=rating&&rating.mode==='DUAL_SHADOW'&&rating.productionActive===false&&rating.productionRating===null&&rating.networkRequests===0&&elo&&glicko&&elo.status==='ESTIMATED_SHADOW'&&glicko.status==='ESTIMATED_SHADOW'&&Number(elo.games)>0&&Number(glicko.games)>0;writeReport({schema:'aram-universal-rating-physical-shadow-v1',state:good?'available':'invalid',history_requests:resolved.history_requests,resolved_matches:resolved.matches.length,dual_shadow:rating&&rating.mode,model_selection:rating&&rating.modelSelection,production_active:false,automatic_promotion:false,production_rating:null,rating_network_requests:rating&&rating.networkRequests,candidates:{elo:elo?{status:elo.status,rating:elo.rating,games:elo.games,confidence:elo.confidence}:null,glicko:glicko?{status:glicko.status,rating:glicko.rating,games:glicko.games,confidence:glicko.confidence}:null},isolated_rating_db:true,privacy:{raw_puuid_returned:false,raw_match_id_returned:false,identity_mapping_returned:false}})}catch(e){fail(e,{history_requests:resolved.history_requests})}}\napp.on('browser-window-created',(_e,win)=>{try{win.webContents.on('did-finish-load',()=>{void run(win)})}catch(e){fail(e)}});\nrequire('./main-v0160.js');\n`}
function build(){fs.rmSync(OUT,{recursive:true,force:true});ensure(OUT);const files=[];for(const [src,dst] of SOURCES)files.push(write(dst,read(src)));files.push(write('main-universal-rating-r1-shadow-rc.js',buildMain()));const manifest={schema:'aram-universal-rating-r1-shadow-rc-kit-v1',production_version:'0.16.0',mode:'temporary_copy_overlay_only',production_install_mutated:false,production_manifest_mutated:false,canonical_research_checkpoint_written:false,rating_database:'isolated_temp_only',max_history_requests_per_run:2,retry:false,automatic_promotion:false,production_rating_active:false,privacy_safe_report:true,files:[...files],sources:SOURCES.map(([src,dst])=>({src,dst,sha256:sha(Buffer.from(read(src),'utf8'))}))};files.push(write('rc-kit.json',JSON.stringify(manifest,null,2)+'\n'));return{...manifest,files}}
if(require.main===module){const report=build();console.log('UNIVERSAL RATING R1 SHADOW RC KIT: BUILT',JSON.stringify({files:report.files.length,mode:report.mode,max_history_requests_per_run:report.max_history_requests_per_run}))}
module.exports={ROOT,OUT,SOURCES,buildMain,build};