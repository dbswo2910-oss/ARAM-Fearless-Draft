'use strict';
const fs=require('fs'),path=require('path');
const ROOT=path.resolve(__dirname,'../..'),R=path.join(ROOT,'research/aram-rating-v032');
let pass=0;const ok=(c,m)=>{if(!c)throw new Error('B5.1 R10 AUDIT: '+m);pass++};
const p=path.join(R,'phase-b51r10-v032-devtools.js'),s=fs.readFileSync(p,'utf8');new Function(s);
ok(s.includes("PHASE='B5.1-R10'"),'phase');
ok(s.includes("CONFIRM='B51R10-COLLECT-THREE-ANCHORS'"),'confirmation token');
ok(s.includes('checkpoint_matches:1984'),'exact R9 match precondition');
ok(s.includes('completed_before:1'),'exact R9 completed precondition');
ok(s.includes('remaining_before:14'),'exact R9 remaining precondition');
ok(s.includes("last_recovery_phase:'B5.1-R9'"),'requires R9 state');
ok(s.includes('max_requests:3'),'three-request cap');
ok(s.includes('retry:false'),'no retry contract');
ok(s.includes('stop_on_first_request_or_validation_failure:true'),'stop-on-failure contract');
ok(s.includes("limit:20,scan:100,queueMode:'standard',priority:'background'"),'known-good request shape');
ok(s.includes('getAramMatchHistory'),'live bridge present');
ok(s.includes("queueId(g)===450")&&s.includes('participantPuuids(g).length===10'),'ARAM validation');
ok(s.includes('targetHits=valid.filter'),'target identity validation');
ok(s.includes("'R10_STOP_REQUEST_FAILED_NO_WRITE'"),'request failure no-write path');
ok(s.includes("'R10_STOP_TARGET_MISMATCH_NO_WRITE'"),'target mismatch no-write path');
ok(s.includes("'R10_CURRENT_ANCHOR_POSTCONDITION_FAILED_ROLLED_BACK'"),'current-anchor rollback path');
ok(s.includes('await writeCp(beforeAnchor)'),'current-anchor rollback write');
ok(s.includes('existingPreserved=beforeMatches.every'),'prior-match preservation check');
ok(s.includes("recovery_phase:'B5.1-R10'"),'recovery logging');
ok(s.includes("status:'bounded_recovery_progress'"),'bounded progress state');
ok(s.includes("classification:progress.length===3?'R10_THREE_ANCHOR_RECOVERY_ACCEPTED'"),'success classification');
ok(!/\.collect\([^)]*B51R10-COLLECT-THREE-ANCHORS/.test(s),'no automatic live run');
ok(!s.includes('deleteDatabase(')&&!s.includes('localStorage.clear('),'no destructive reset');
ok(!s.includes('raw.githubusercontent.com')&&!s.includes('(0,eval)'),'no remote code fetch/eval');
const manifest=JSON.parse(fs.readFileSync(path.join(ROOT,'update/manifest.json'),'utf8'));
ok(!(manifest.files||[]).some(x=>String(x.source||'').includes('phase-b51r10-v032-devtools')),'R10 not shipped');
const report={status:'SUCCESS',passes:pass,phase:'B5.1-R10',start_checkpoint_matches:1984,start_completed:1,start_remaining:14,max_requests:3,retry:false,stop_on_first_failure:true,per_anchor_commit:true,current_anchor_rollback:true,production_changed:false,automatic_run:false};
fs.mkdirSync(path.join(ROOT,'audit-output'),{recursive:true});fs.writeFileSync(path.join(ROOT,'audit-output/aram-rating-v032-b51r10-recovery-audit.json'),JSON.stringify(report,null,2)+'\n');console.log(`B5.1 R10 GUARDED THREE-ANCHOR RECOVERY AUDIT: SUCCESS · ${pass} checks`);
