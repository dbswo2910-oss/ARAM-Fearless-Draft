'use strict';
const fs=require('fs'),path=require('path'),crypto=require('crypto');
const ROOT=path.resolve(__dirname,'../..');
const OUT=path.join(ROOT,'audit-output','r26-shadow-evidence-inspector-kit');
const MODULES=Object.freeze([
  ['shadow-evidence-store','src/research/shadow-evidence-store.js'],
  ['shadow-promotion-gate','src/research/shadow-promotion-gate.js']
]);
const sha=b=>crypto.createHash('sha256').update(b).digest('hex');
const read=rel=>fs.readFileSync(path.join(ROOT,rel),'utf8');
const ensure=d=>fs.mkdirSync(d,{recursive:true});
function write(name,body){const p=path.join(OUT,name);ensure(path.dirname(p));fs.writeFileSync(p,body,'utf8');return{file:name,sha256:sha(Buffer.from(body,'utf8')),bytes:Buffer.byteLength(body)}}
function buildRenderer(){
  const entries=MODULES.map(([id,rel])=>`${JSON.stringify(id)}:function(module,exports,require){\n${read(rel)}\n}`).join(',\n');
  return `(function(){\n'use strict';\nconst __mods={${entries}};\nconst __cache={};\nfunction __norm(id){return String(id||'').replace(/^\\.\\//,'').replace(/\\.js$/,'')}\nfunction __req(id){id=__norm(id);if(__cache[id])return __cache[id].exports;const fn=__mods[id];if(!fn)throw new Error('R26 module unavailable: '+id);const module={exports:{}};__cache[id]=module;fn(module,module.exports,__req);return module.exports}\nconst MARKER='[ARAM_R26_SHADOW_EVIDENCE_RESULT]';\nconst finite=x=>Number.isFinite(Number(x))?Number(x):null;\nfunction snapshotSummary(x){return {matches:Number(x?.dataset?.matches)||0,players:Number(x?.dataset?.players)||0,fingerprint:String(x?.dataset?.fingerprint||''),observed_leader:x?.observed_leader||null,observed_runner_up:x?.observed_runner_up||null,selection_status:x?.selection_status||null,frozen_gap_glicko_minus_elo:finite(x?.metrics?.log_loss_gap_glicko_minus_elo?.frozen),walk_forward_gap_glicko_minus_elo:finite(x?.metrics?.log_loss_gap_glicko_minus_elo?.walk_forward)}}\nasync function run(){\n  const store=__req('shadow-evidence-store'),gate=__req('shadow-promotion-gate');\n  const repo=store.createIndexedDbRepository({indexedDBRef:window.indexedDB});\n  const matches=await repo.listMatches(),snapshots=await repo.listSnapshots();\n  const promotion=gate.evaluatePromotion(snapshots);\n  const latest=snapshots.at(-1)||null,first=snapshots[0]||null;\n  const result={schema:'aram-rating-r26-shadow-evidence-inspector-v1',state:'available',read_only:true,riot_lcu_requests:0,checkpoint_writes:0,production_score_writes:0,ui_writes:0,raw_identity_returned:false,delta_matches:matches.length,snapshot_count:snapshots.length,first_snapshot:first?snapshotSummary(first):null,latest_snapshot:latest?snapshotSummary(latest):null,promotion:{status:promotion.status,observed_leader:promotion.observed_leader,distinct_snapshots:promotion.distinct_snapshots,match_growth:promotion.match_growth,latest_selection_status:promotion.latest_selection_status,conditions:promotion.conditions,next_step:promotion.next_step,production_activation_authorized:false,automatic_promotion:false},recent_snapshots:snapshots.slice(-5).map(snapshotSummary)};\n  try{Object.defineProperty(window,'__ARAM_R26_SHADOW_EVIDENCE__',{value:Object.freeze(result),configurable:true})}catch{}\n  console.log(MARKER+JSON.stringify(result));return result;\n}\nPromise.resolve().then(run).catch(e=>console.log(MARKER+JSON.stringify({schema:'aram-rating-r26-shadow-evidence-inspector-v1',state:'error',read_only:true,riot_lcu_requests:0,error:String(e?.message||e),raw_identity_returned:false,production_activation_authorized:false})));\nreturn{installed:true,mode:'r26-read-only-shadow-evidence'};\n})()\n//# sourceURL=aram-rating-r26-shadow-evidence-inspector.js\n`;
}
function buildMain(){return `'use strict';\nconst fs=require('fs'),path=require('path');\nconst {app}=require('electron');\nconst MARKER='[ARAM_R26_SHADOW_EVIDENCE_RESULT]';\nconst renderer=fs.readFileSync(path.join(__dirname,'r26-shadow-evidence-renderer.js'),'utf8');\nconst reportPath=String(process.env.ARAM_R26_REPORT||'').trim();\nlet written=false,quitScheduled=false;\nfunction quit(){if(quitScheduled)return;quitScheduled=true;setTimeout(()=>{try{app.quit()}catch{}},300)}\nfunction save(result){if(!reportPath||written)return;try{fs.mkdirSync(path.dirname(reportPath),{recursive:true});fs.writeFileSync(reportPath,JSON.stringify({status:result&&result.state==='available'?'SUCCESS':'FAILURE',stage:'R26_SHADOW_EVIDENCE_INSPECT',captured_at:new Date().toISOString(),result},null,2)+'\\n','utf8');written=true;quit()}catch(e){console.warn('[R26] report write failed',e&&e.message||String(e))}}\napp.on('browser-window-created',(_event,win)=>{\n  try{win.webContents.on('console-message',(_e,...args)=>{const msg=args.find(x=>typeof x==='string'&&x.startsWith(MARKER));if(!msg)return;try{save(JSON.parse(msg.slice(MARKER.length)))}catch(e){console.warn('[R26] parse failed',e&&e.message||String(e))}})}catch{}\n  const inject=()=>{try{Promise.resolve(win.webContents.executeJavaScript(renderer,false)).catch(e=>console.warn('[R26] inject failed',e&&e.message||String(e)))}catch(e){console.warn('[R26] inject failed',e&&e.message||String(e))}};\n  try{win.webContents.on('did-finish-load',inject)}catch{}\n});\nrequire('./main-v0160.js');\n`}
function build(){fs.rmSync(OUT,{recursive:true,force:true});ensure(OUT);const files=[];files.push(write('r26-shadow-evidence-renderer.js',buildRenderer()));files.push(write('main-r26-shadow-evidence.js',buildMain()));const manifest={schema:'aram-rating-r26-shadow-evidence-inspector-kit-v1',mode:'temporary_copy_read_only',production_install_mutated:false,production_manifest_mutated:false,riot_lcu_requests:0,checkpoint_writes:0,production_score_writes:0,raw_identity_export:false,files:[...files],source_modules:MODULES.map(([id,rel])=>({id,rel,sha256:sha(Buffer.from(read(rel),'utf8'))}))};files.push(write('kit.json',JSON.stringify(manifest,null,2)+'\n'));return{...manifest,files}}
if(require.main===module){const r=build();console.log('R26 SHADOW EVIDENCE INSPECTOR KIT: BUILT',JSON.stringify({files:r.files.length,mode:r.mode,riot_lcu_requests:0}))}
module.exports={ROOT,OUT,MODULES,buildRenderer,buildMain,build};
