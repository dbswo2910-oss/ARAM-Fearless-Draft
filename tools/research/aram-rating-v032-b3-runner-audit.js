'use strict';
const fs=require('fs'),path=require('path');
const ROOT=path.resolve(__dirname,'../..'),R=path.join(ROOT,'research/aram-rating-v032');
let pass=0;const ok=(c,m)=>{if(!c)throw new Error('B3 RUNNER AUDIT: '+m);pass++};
const p=path.join(R,'phase-b3-v032-devtools.js'),s=fs.readFileSync(p,'utf8');new Function(s);
ok(s.includes("CONFIRM='B3-DENSITY-FIRST-1000'"),'confirmation token');
ok(s.includes('max_expanded_players:50'),'expanded-player cap');
ok(s.includes('max_matches_per_player:20'),'per-player cap');
ok(s.includes('max_total_accepted_matches:1000'),'total match cap');
ok(s.includes('max_requests:150'),'request cap');
ok(s.includes('min_resume_matches:500'),'resume floor');
ok(s.includes('dynamic_rerank:true'),'dynamic rerank');
ok(s.includes('explicit_confirmation_required'),'explicit confirmation');
ok(s.includes('completed_puuids_b3_v032'),'B3-only completion state');
ok(s.includes('NO Riot/LCU collection request has started'),'load is inert');
ok(!/\brunB3\(\)\s*;/.test(s),'no automatic run');
ok(!s.includes('deleteDatabase(')&&!s.includes('localStorage.clear('),'no destructive reset');
ok(!s.includes('raw.githubusercontent.com')&&!s.includes('(0,eval)'),'no remote code fetch/eval');
const manifest=JSON.parse(fs.readFileSync(path.join(ROOT,'update/manifest.json'),'utf8'));
ok(manifest.version==='0.16.0','production version');
ok(!(manifest.files||[]).some(x=>String(x.source||'').includes('phase-b3-v032-devtools')),'runner not shipped');
const report={status:'SUCCESS',passes:pass,phase:'B3',resume_min_matches:500,target_matches:1000,max_expanded_players:50,max_matches_per_player:20,max_requests:150,explicit_confirmation:true,automatic_run:false,remote_eval:false,production_changed:false};
fs.mkdirSync(path.join(ROOT,'audit-output'),{recursive:true});fs.writeFileSync(path.join(ROOT,'audit-output/aram-rating-v032-b3-runner-audit.json'),JSON.stringify(report,null,2)+'\n');
console.log(`B3 RUNNER AUDIT: SUCCESS · ${pass} checks`);
