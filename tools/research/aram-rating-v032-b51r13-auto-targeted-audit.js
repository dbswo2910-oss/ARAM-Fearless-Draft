'use strict';
const fs=require('fs'),path=require('path');
const ROOT=path.resolve(__dirname,'../..'),R=path.join(ROOT,'research/aram-rating-v032');
let pass=0;const ok=(c,m)=>{if(!c)throw new Error('B5.1 R13 AUDIT: '+m);pass++};
const p=path.join(R,'phase-b51r13-v032-auto-targeted-devtools.js'),s=fs.readFileSync(p,'utf8');new Function(s);
ok(s.includes("PHASE='B5.1-R13'"),'phase');
ok(s.includes("CONFIRM='B51R13-AUTO-TARGETED-THREE'"),'explicit confirm token');
ok(s.includes('max_requests:3'),'bounded to 3 requests');
ok(s.includes('max_consecutive_zero_new:2'),'zero-yield early stop');
ok(s.includes('retry:false'),'no retry contract');
ok(s.includes('stop_on_candidate_winner:true'),'candidate winner early stop');
ok(s.includes("initialRun.selection?.status||'')!=='no_clear_winner'"),'requires no-clear-winner state');
ok(s.includes('rankTargets'),'re-ranks targets');
ok(s.includes('targeted_information_gain_score'),'records targeted IG score');
ok(s.includes('buildLatestRun'),'re-evaluates model');
ok(s.includes("'candidate_winner'"),'candidate gate checked');
ok(s.includes('getAramMatchHistory'),'uses live history bridge');
ok(s.includes('request_budget_consumed_reload_required'),'single explicit run per load');
ok(s.includes("'readwrite'"),'checkpoint write is explicit');
ok(s.includes('existing_match_integrity'),'preserves prior matches');
ok(s.includes('ROLLED_BACK'),'rollback path');
ok(s.includes('raw_puuid_returned:false'),'no raw puuid result');
ok(s.includes('production_changed:false'),'production unchanged');
ok(!s.includes('deleteDatabase(')&&!s.includes('localStorage.clear('),'no destructive reset');
const manifest=JSON.parse(fs.readFileSync(path.join(ROOT,'update/manifest.json'),'utf8'));
ok(!(manifest.files||[]).some(x=>String(x.source||'').includes('phase-b51r13-v032-auto-targeted-devtools')),'R13 not shipped');
const report={status:'SUCCESS',passes:pass,phase:'B5.1-R13',request_budget:3,retry:false,production_changed:false,automatic_run:false};
fs.mkdirSync(path.join(ROOT,'audit-output'),{recursive:true});fs.writeFileSync(path.join(ROOT,'audit-output/aram-rating-v032-b51r13-auto-targeted-audit.json'),JSON.stringify(report,null,2)+'\n');
console.log(`B5.1 R13 AUTO-TARGETED AUDIT: SUCCESS · ${pass} checks`);
