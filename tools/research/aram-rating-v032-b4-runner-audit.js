'use strict';
const fs=require('fs'),path=require('path');
const ROOT=path.resolve(__dirname,'../..'),R=path.join(ROOT,'research/aram-rating-v032');
let pass=0;const ok=(c,m)=>{if(!c)throw new Error('B4 RUNNER AUDIT: '+m);pass++};
const p=path.join(R,'phase-b4-v032-devtools.js'),s=fs.readFileSync(p,'utf8');new Function(s);
ok(s.includes("CONFIRM='B4-MATURE-NETWORK-2000'"),'confirmation token');
ok(s.includes('max_expanded_players:60'),'expanded-player cap');
ok(s.includes('max_matches_per_player:20'),'per-player cap');
ok(s.includes('max_total_accepted_matches:2000'),'total match cap');
ok(s.includes('max_requests:180'),'request cap');
ok(s.includes('min_candidate_observations:5'),'mature candidate floor');
ok(s.includes('min_resume_matches:1000'),'resume floor');
ok(s.includes('dynamic_rerank:true'),'dynamic rerank');
ok(s.includes('automatic_collection:false'),'automatic collection disabled');
ok(s.includes('explicit_confirmation_required'),'explicit confirmation');
ok(s.includes('completed_puuids_b4_v032'),'B4-only completion state');
ok(s.includes('buildMatureCandidatePool'),'mature pool used');
ok(s.includes('mature_neighbor_ratio'),'mature-network telemetry');
ok(s.includes('ten_plus_neighbor_ratio'),'10+ telemetry');
ok(s.includes('no_cold_start_potential'),'no-cold-start telemetry');
ok(s.includes('NO Riot/LCU collection request has started'),'load is inert');
ok(!/\brunB4\(\)\s*;/.test(s),'no automatic run');
ok(!s.includes('deleteDatabase(')&&!s.includes('localStorage.clear('),'no destructive reset');
ok(!s.includes('raw.githubusercontent.com')&&!s.includes('(0,eval)'),'no remote code fetch/eval');
const manifest=JSON.parse(fs.readFileSync(path.join(ROOT,'update/manifest.json'),'utf8'));
ok(manifest.version==='0.16.0','production version');
ok(!(manifest.files||[]).some(x=>String(x.source||'').includes('phase-b4-v032-devtools')),'runner not shipped');
const report={status:'SUCCESS',passes:pass,phase:'B4',profile:'B4_MATURE_NETWORK_2000',resume_min_matches:1000,target_matches:2000,min_candidate_observations:5,max_expanded_players:60,max_matches_per_player:20,max_requests:180,explicit_confirmation:true,automatic_run:false,automatic_collection:false,remote_eval:false,production_changed:false};
fs.mkdirSync(path.join(ROOT,'audit-output'),{recursive:true});fs.writeFileSync(path.join(ROOT,'audit-output/aram-rating-v032-b4-runner-audit.json'),JSON.stringify(report,null,2)+'\n');
console.log(`B4 RUNNER AUDIT: SUCCESS · ${pass} checks`);
