'use strict';
const fs=require('fs');
const path=require('path');
const crypto=require('crypto');
const ROOT=path.resolve(__dirname,'../..');
const OUT=path.join(ROOT,'audit-output','universal-rating-r4-shipped-shadow-physical-harness');
const sha=b=>crypto.createHash('sha256').update(b).digest('hex');
function ensure(d){fs.mkdirSync(d,{recursive:true})}
function buildMain(){return `'use strict';\nconst fs=require('fs'),path=require('path');\nconst {app}=require('electron');\nconst reportPath=String(process.env.ARAM_UNIVERSAL_R4_REPORT||'').trim();\nconst dbRoot=String(process.env.ARAM_UNIVERSAL_RATING_DB_ROOT||'').trim();\nlet started=false,written=false;\nfunction sleep(ms){return new Promise(r=>setTimeout(r,ms))}\nfunction quit(){setTimeout(()=>{try{app.quit()}catch{}},300)}\nfunction write(result){if(written)return;written=true;const envelope={status:result&&result.state==='available'?'SUCCESS':'FAILURE',stage:'UNIVERSAL_RATING_R4_SHIPPED_SHADOW_PHYSICAL',captured_at:new Date().toISOString(),result};if(reportPath){fs.mkdirSync(path.dirname(reportPath),{recursive:true});fs.writeFileSync(reportPath,JSON.stringify(envelope,null,2)+'\\n','utf8')}try{console.log('[UNIVERSAL_RATING_R4_RESULT]'+JSON.stringify(result))}catch{}quit()}\nfunction fail(e,extra={}){write({schema:'aram-universal-rating-shipped-shadow-physical-v1',state:'error',error_name:e&&e.name||'Error',error_message:String(e&&e.message||e||'unknown_error').slice(0,240),history_requests:Number(extra.history_requests)||0,production_active:false,automatic_promotion:false,production_rating:null,privacy:{raw_puuid_returned:false,raw_match_id_returned:false}})}\nasync function waitForRating(puuid){const file=path.join(dbRoot,'rating','universal-rating-v1.json');const deadline=Date.now()+15000;while(Date.now()<deadline){try{if(fs.existsSync(file)){const s=JSON.parse(fs.readFileSync(file,'utf8'));const rows=Object.values(s.ratings||{}).filter(x=>String(x&&x.puuid||'')===puuid);const elo=rows.find(x=>x&&x.modelName==='elo'),glicko=rows.find(x=>x&&x.modelName==='glicko');if(elo&&glicko&&Number.isFinite(Number(elo.rating))&&Number.isFinite(Number(glicko.rating))&&Number(elo.games)>0&&Number(glicko.games)>0)return{state:s,elo,glicko,file}}}catch{}await sleep(250)}throw new Error('shipped shadow rating records were not persisted within 15 seconds')}\nasync function run(win){if(started)return;started=true;if(!dbRoot)return fail(new Error('ARAM_UNIVERSAL_RATING_DB_ROOT required'));let resolved;try{resolved=await win.webContents.executeJavaScript(\`(async()=>{const b=window.aramDesktop;if(!b||typeof b.getAramMatchHistory!=='function')return{ok:false,reason:'match_history_bridge_unavailable'};const r=await b.getAramMatchHistory({limit:20,scan:100,target:{current:true},queueMode:'standard',priority:'interactive'});const puuid=String(r&&r.account&&r.account.puuid||'').trim();return{ok:!!(r&&r.connected!==false&&puuid&&Array.isArray(r.matches)&&r.matches.length),reason:'history_unavailable',puuid,matches:Array.isArray(r&&r.matches)?r.matches.length:0};})()\`,false)}catch(e){return fail(e,{history_requests:1})}if(!resolved||resolved.ok!==true)return fail(new Error(resolved&&resolved.reason||'history_unavailable'),{history_requests:1});try{const persisted=await waitForRating(String(resolved.puuid));const elo=persisted.elo,glicko=persisted.glicko;const matches=Object.keys(persisted.state.matches||{}).length;const good=elo&&glicko&&elo.productionActive===false&&glicko.productionActive===false&&matches>0;write({schema:'aram-universal-rating-shipped-shadow-physical-v1',state:good?'available':'invalid',candidate_version:'0.16.1',history_requests:1,resolved_matches:Number(resolved.matches)||0,persisted_matches:matches,automatic_sidecar:true,rating_network_requests:0,production_active:false,automatic_promotion:false,production_rating:null,candidates:{elo:{status:elo.status,rating:elo.rating,games:elo.games,confidence:elo.confidence},glicko:{status:glicko.status,rating:glicko.rating,games:glicko.games,confidence:glicko.confidence}},isolated_user_data:true,isolated_rating_db:true,privacy:{raw_puuid_returned:false,raw_match_id_returned:false,identity_mapping_returned:false}})}catch(e){fail(e,{history_requests:1})}}\napp.on('browser-window-created',(_e,win)=>{try{win.webContents.on('did-finish-load',()=>{void run(win)})}catch(e){fail(e)}});\nrequire('./main-v0161-shadow.js');\n`}
function build(){fs.rmSync(OUT,{recursive:true,force:true});ensure(OUT);const body=buildMain(),file=path.join(OUT,'main-r4-shipped-shadow-physical.js');fs.writeFileSync(file,body,'utf8');const report={schema:'aram-universal-rating-r4-physical-harness-v1',status:'BUILT',candidate_version:'0.16.1',history_requests_per_run:1,retry:false,production_install_mutated:false,production_manifest_mutated:false,isolated_user_data:true,privacy_safe_report:true,file:'main-r4-shipped-shadow-physical.js',sha256:sha(Buffer.from(body,'utf8')),bytes:Buffer.byteLength(body)};fs.writeFileSync(path.join(OUT,'harness.json'),JSON.stringify(report,null,2)+'\n','utf8');return report}
if(require.main===module){const r=build();console.log('UNIVERSAL RATING R4 SHIPPED SHADOW PHYSICAL HARNESS: BUILT',JSON.stringify({candidate:r.candidate_version,history_requests:r.history_requests_per_run,isolated_user_data:r.isolated_user_data}))}
module.exports={ROOT,OUT,buildMain,build};
